<h1>Editing Book</h1>
<%= form_for(@book) do |f| %>
<% if f.object.errors.any? %>
  <div class="alert alert-warning">
    <h3><%= pluralize(f.object.errors.count, "error") %> prohibited this obj from being saved:</h3>
    <ul>
      <% f.object.errors.full_messages.each do |message| %>
        <li><%= message %></li>
    </div>
    <% end %>
  <% end %>
           <h4>Title</h4>
        <%= f.text_field :title, class:"btn-block" %>
           <h4>Opinion</h4>
      <%= f.text_area :body, class:"btn-block" %><br>
  <br><%= f.submit "Update Book", class: "btn btn-primary btn-block" %>
<% end %>
<%= link_to "Show", book_path(@book.id) %>
<a> | </a>
<%= link_to "Back", books_path %>
