
<h2>User info</h2>
<!--div class="profile-container"-->

  <%= attachment_image_tag user, :profile_image, :fill, 60, 60, fallback: "no_image.jpg", format: 'jpeg', class: "profile-img" %>

<table class="table">
     <tbody>
      <tr>
        <th>name</th>
        <th><%= user.name %></th>
    </tr>
    <tr>
      <th>introduction</th>
        <th><%= user.introduction %></th>
      </tr>
  </tbody>
</table>
<%= link_to edit_user_path(user.id), class: "btn btn-default btn-block" do %>
    <span class="glyphicon glyphicon-wrench"></span>
  <% end %>
  <% if @user.id == current_user.id %>
  <% end %>



  <h1>New book</h1>
<%= form_for(@book) do |f| %>
<% if f.object.errors.any? %>
  <div class="alert alert-warning">
    <h3><%= pluralize(f.object.errors.count, "error") %> prohibited this obj from being saved:</h3>
    <ul>
      <% f.object.errors.full_messages.each do |message| %>
        <li><%= message %></li>
    </div>
    <% end %>
  <% end %>
<h4>Title</h4>
        <%= f.text_field :title %>
<h4>Opinion</h4>
      <%= f.text_area :body %><br>
  <br><%= f.submit "Create Book", class: "btn btn-primary btn-block" %>
<% end %>
</div>